# Component showcase / documentation

Type: grilling
Status: open
Blocked by: 02

## Question

With the v1 inventory named (ticket 05: ~30 components across P0/P1/P2, spec docs in `packages/design-system/docs/components/`), decide the showcase: Storybook for the web (MUI-wrapped) implementations — where it lives in nx, whether it doubles as the visual-regression surface — and what, if anything, showcases the RN/Compose/SwiftUI implementations (Expo storybook? native preview apps? nothing in v1, relying on the anatomy spec docs + screenshots). Depends on the MUI theming strategy (ticket 02) for how stories get the Lumira theme + white-label palette switcher.
