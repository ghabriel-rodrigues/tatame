/** professor turmas tab (AUTH.20 empty shell). */

import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export default function ProfessorTurmasScreen() {
  return <PlaceholderScreen title="Turmas" />;
}
