/** professor alunos tab (AUTH.20 empty shell). */

import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export default function ProfessorAlunosScreen() {
  return <PlaceholderScreen title="Alunos" />;
}
