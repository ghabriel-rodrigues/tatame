/** aluno perfil tab (AUTH.20): session identity + "Sair" (logout). */

import { ProfileScreen } from '../../../components/ProfileScreen';

export default function AlunoPerfilScreen() {
  return <ProfileScreen />;
}
