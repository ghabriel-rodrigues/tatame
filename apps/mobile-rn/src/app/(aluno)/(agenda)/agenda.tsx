/** aluno agenda tab (AUTH.20 empty shell). */

import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export default function AlunoAgendaScreen() {
  return <PlaceholderScreen title="Agenda" />;
}
