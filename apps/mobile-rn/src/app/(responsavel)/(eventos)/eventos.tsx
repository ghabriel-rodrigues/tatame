/** responsavel eventos tab (AUTH.20 empty shell). */

import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export default function ResponsavelEventosScreen() {
  return <PlaceholderScreen title="Eventos" />;
}
